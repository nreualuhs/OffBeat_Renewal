/** EndGameImpostor */

import React from 'react';
import { useNavigate } from 'react-router-dom';

export default function EndGameImpostor() {
    const navigate = useNavigate();

    return (
        <h1>Welcome! This is the Impostors End page</h1>
    );
}